{
  "name": "gm-landing-page",
  "version": "0.2.1",
  "private": true,
  "dependencies": {
    "gm-react-scripts": "0.1.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "styled-components": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build-docker":
      "docker build -t deciphernow/gm-landing-page -f docker-prod/Dockerfile .",
    "publish": "node scripts/publish.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "commander": "^2.15.1",
    "prompt": "^1.0.0"
  }
}
